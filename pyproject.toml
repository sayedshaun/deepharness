[project]
name = "subagents"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx", "pydantic"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.ruff]
line-length = 88
target-version = "py310"

